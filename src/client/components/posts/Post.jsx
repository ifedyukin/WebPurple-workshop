import React from 'react';
import { Link } from 'react-router-dom';
import { convertDate } from '../../helpers';
import '../../styles/post.css';

export const Post = ({ id, title, createdAt, url, body, type, full = false }) => (
  <div className="post">
    {/* описываем разметку поста */}
  </div>
);
